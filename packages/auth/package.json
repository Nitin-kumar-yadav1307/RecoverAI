{
  "name": "@recoverai/auth",
  "version": "0.1.0",
  "private": true,
  "description": "RecoverAI authentication primitives: password hashing, JWT tokens, and merchant-isolation authorization (zero-dependency, uses Node crypto)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@recoverai/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^22.7.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}